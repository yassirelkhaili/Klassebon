# PostgreSQL
# From repo root: docker compose up -d
# Must match docker-compose.yml (user/db/password: klassebon):
DATABASE_URL="postgresql://klassebon:klassebon@localhost:5432/klassebon?schema=public"
# If you use a different Postgres, replace user/password/host/db, not the literal "USER:PASSWORD" placeholders.

# Better Auth (secret min 32 chars)
BETTER_AUTH_SECRET="hATvCpJDB9Sg4TmBTtn/o/jivzdd8IamGGIiEVB1rdU="
BETTER_AUTH_URL="http://localhost:3000"

# CORS / client
FRONTEND_ORIGIN="http://localhost:5173"

# Express api port
PORT=3000

# Mailpit mock SMTP
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_FROM="KlasseBon <noreply@localhost>"
MAILPIT_WEB_URL="http://localhost:8025"
